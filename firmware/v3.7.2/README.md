# v3.7.2 Release Notes

## Config Fixes
- **UI Refinement**: Increased the size of the **Service** and **Settings** buttons on the dashboard from 40x40 to 60x60 pixels. This makes them easier to press and visually consistent with the navigation buttons.

## Binaries
- `bar_gauge_demo.bin`: Application Firmware.
- `bootloader.bin`: Bootloader.
- `partition-table.bin`: Partition Table.

## Flashing Instructions
Flash at address `0x10000`.
